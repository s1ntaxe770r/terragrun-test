generate_projects:
  blocks:
    - block_name: infra
      root_dir: "infra/"
      workflow: default

reporting:
  ai_summary: true
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
