projects:
  - name: drift-blame-demo
    dir: infra/dev/my-project/iam/permissions/secret-access
    workflow: default
    drift_detection: true
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
pr_locks: false
