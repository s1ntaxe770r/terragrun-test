projects:
  - name: drift-truncation-demo
    dir: infra/dev/my-project
    workflow: default
    drift_detection: true
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
pr_locks: false
