generate_projects:
  drift_terragrunt_parallelism: 20
  blocks:
    - block_name: tg
      terragrunt: true
      root_dir: tg
reporting:
  ai_summary: true
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
pr_locks: false
